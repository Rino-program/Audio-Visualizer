{
  "name": "audio-visualizer-android",
  "version": "1.0.12",
  "description": "Audio Visualizer - Android APK版",
  "main": "index.js",
  "scripts": {
    "dev": "npx cap open android",
    "sync": "npx cap sync",
    "build": "npx cap sync && cd android && gradlew.bat assembleDebug",
    "build-release": "npx cap sync && cd android && gradlew.bat assembleRelease"
  },
  "keywords": ["audio", "visualizer", "android", "capacitor"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0",
    "@capawesome/capacitor-file-picker": "^6.0.1"
  }
}
